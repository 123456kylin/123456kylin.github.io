# git
```
git init 
git config use.email ""
git config use.name ""
git commit -a -m "init"
git config --global core.autocrlf false
git status 
git clone url
git checkout 恢复已修改的文件
git rm --cached 指定删除的文件
```